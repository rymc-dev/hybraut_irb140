# Pip-only dependencies for hybraut_irb140 (packages with no rosdep key).
# ROS dependencies are declared in package.xml and installed via `rosdep install`.
#
# torch / torchvision are installed separately by the Dockerfile from the PyTorch
# CPU wheel index (--index-url https://download.pytorch.org/whl/cpu) so the large
# CUDA build is never pulled in. Install those first if using this file by hand:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
#   pip install -r requirements.txt

ultralytics
hybrid-automaton
